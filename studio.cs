var ifOpen = func (string day) =>
{
	return day + "is open";
}
public static int toPay(int count)
{
	if (count > 0)
	{
		return 100 * count;
	}
	else
		return 0;
}
int count = 6;
int x = 100;
int sum = count * x;
int y = sum;