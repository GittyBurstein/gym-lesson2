using System.Runtime.Remoting.Messaging;

const ifOpen = func (string day) =>return day+"is open";

public static bool(int age)
var ifOpen = func (string day) =>
{
	if(age>18)
	   return true;
    return false;
	return day + "is open";
}
public static int toPay(int count)
{
	if (count > 0)
	{
		return 100 * count;
	}
	else
		return 0;
}
int count = 6;
int x = 100;
int sum = count * x;
int y = sum;